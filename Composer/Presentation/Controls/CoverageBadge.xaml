<UserControl x:Class="Composer.Presentation.Controls.CoverageBadge"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid x:Name="HostGrid" Visibility="Collapsed">
    <StackPanel Spacing="6">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Style="{StaticResource EyebrowText}"
                   Text="COVERAGE"
                   VerticalAlignment="Center" />
        <TextBlock Grid.Column="1"
                   x:Name="CounterText"
                   Style="{StaticResource BodySmallText}"
                   Foreground="{ThemeResource Ink3Brush}"
                   VerticalAlignment="Center" />
      </Grid>

      <ItemsControl x:Name="DotsHost">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal" Spacing="4" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>

      <StackPanel Orientation="Horizontal"
                  Spacing="6"
                  x:Name="ChipsRow"
                  Visibility="Collapsed" />
    </StackPanel>
  </Grid>
</UserControl>
